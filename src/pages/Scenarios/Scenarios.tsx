import React from "react";
import { SearchBar } from "../../components";

const Scenarios = () => {
  return (
    <div>
      <h1>Scenarios</h1>
      <SearchBar />
    </div>
  );
};

export default Scenarios;
